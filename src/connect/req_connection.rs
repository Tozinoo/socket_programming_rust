// use std::net::SocketAddr;
// use mio::net::{TcpListener, TcpStream};
// use mio::Poll;
//
// pub fn req_connection (server_addr: SocketAddr) {
//     let mut stream = TcpStream::connect(server_addr);
//     let mut poll = Poll::new().expect("poll error");
//
// }